name: DEN-2037 private lock root repair

on:
  push:
    branches: [den-2037-private-lock-root]
    paths: ['.github/workflows/den-2037-private-lock-root-repair.yml']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair-and-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: den-2037-private-lock-root
          fetch-depth: 0
          persist-credentials: true
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
        with:
          toolchain: '1.88.0'
          components: 'rustfmt,clippy'
      - name: Enforce private Unix lock roots and add regression
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p = Path('src/lib.rs')
          text = p.read_text()
          old = '''fn open_lock_file(path: &Path) -> Result<(File, PathBuf)> {
              if let Some(parent) = path.parent() {
                  fs::create_dir_all(parent)
                      .with_context(|| format!("creating lock directory {}", parent.display()))?;
              }
          '''
          new = '''fn open_lock_file(path: &Path) -> Result<(File, PathBuf)> {
              if let Some(parent) = path.parent() {
                  fs::create_dir_all(parent)
                      .with_context(|| format!("creating lock directory {}", parent.display()))?;
                  harden_lock_root_permissions(parent)?;
              }
          '''
          if old not in text:
              raise SystemExit('open_lock_file anchor not found')
          text = text.replace(old, new, 1)
          insert = '''
          #[cfg(unix)]
          fn harden_lock_root_permissions(parent: &Path) -> Result<()> {
              use std::os::unix::fs::PermissionsExt;

              let metadata = fs::symlink_metadata(parent)
                  .with_context(|| format!("inspecting lock directory {}", parent.display()))?;
              if !metadata.file_type().is_dir() || metadata.file_type().is_symlink() {
                  bail!("lock directory {} is not a real directory", parent.display());
              }
              let current = metadata.permissions().mode() & 0o777;
              if current & 0o077 != 0 {
                  let mut permissions = metadata.permissions();
                  permissions.set_mode(0o700);
                  fs::set_permissions(parent, permissions)
                      .with_context(|| format!("restricting lock directory {} to mode 0700", parent.display()))?;
              }
              Ok(())
          }

          #[cfg(not(unix))]
          fn harden_lock_root_permissions(_parent: &Path) -> Result<()> {
              Ok(())
          }

          '''
          marker = 'fn canonical_lock_path(path: &Path) -> Result<PathBuf> {'
          if marker not in text:
              raise SystemExit('canonical_lock_path anchor not found')
          text = text.replace(marker, insert + marker, 1)
          p.write_text(text)

          tests = Path('tests/private_lock_root.rs')
          tests.write_text('''use std::fs;
          use std::time::{SystemTime, UNIX_EPOCH};

          use zed_lock::{LockManager, LockRequest};

          #[cfg(unix)]
          #[test]
          fn lock_root_is_private_even_when_seeded_wide() {
              use std::os::unix::fs::PermissionsExt;

              let nonce = SystemTime::now()
                  .duration_since(UNIX_EPOCH)
                  .expect("clock")
                  .as_nanos();
              let root = std::env::temp_dir().join(format!(
                  "zed-lock-private-root-{}-{nonce}",
                  std::process::id()
              ));
              fs::create_dir_all(&root).expect("seed root");
              let mut permissions = fs::metadata(&root).expect("root metadata").permissions();
              permissions.set_mode(0o755);
              fs::set_permissions(&root, permissions).expect("widen seeded root");

              let path = root.join("install.lock");
              let guard = LockManager::global()
                  .acquire_blocking(LockRequest::exclusive(&path).operation("privacy regression"))
                  .expect("acquire lock");
              let mode = fs::metadata(&root)
                  .expect("root metadata after acquire")
                  .permissions()
                  .mode()
                  & 0o777;
              assert_eq!(
                  mode & 0o077,
                  0,
                  "lock root remained group/other accessible: {mode:o}"
              );
              drop(guard);
              fs::remove_dir_all(&root).expect("cleanup root");
          }
          ''')
          PY
          cargo fmt --all
          cp Cargo.lock "$RUNNER_TEMP/Cargo.lock.before"
          cargo test --test private_lock_root -- --nocapture
          cargo clippy --all-targets -- -D warnings
          cmp "$RUNNER_TEMP/Cargo.lock.before" Cargo.lock || cp "$RUNNER_TEMP/Cargo.lock.before" Cargo.lock
      - name: Commit hardened implementation
        run: |
          set -euo pipefail
          git add src/lib.rs tests/private_lock_root.rs
          git diff --cached --check
          git diff --exit-code -- Cargo.lock
          git config user.name 'ORESoftware'
          git config user.email 'alex@oresoftware.com'
          git commit -m 'fix(DEN-2037): keep local lock roots private on Unix'
          git push origin HEAD:den-2037-private-lock-root
